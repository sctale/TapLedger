# 更新日志

## [0.5.4] - 2026-08-16

### 优化

- **热力图配色**：记账日期色块由靛蓝色系改为暖橙色系（消费语义），与选中日期的深蓝实底明显区分，不再混淆
- **切 Tab 回顶**：四个页面改为常驻挂载后，切换 Tab 时自动滚动回顶部（此前会保留上次浏览位置）

### 修复

- **新增自定义分类不生效**：首页分类选择器常驻挂载不重渲染，添加/删除分类后新分类不可见；现监听分类变更事件自动刷新

## [0.5.3] - 2026-08-16

### 修复（明细页）

- **选中日期数字不可见**：选中格背景仍是浅热力色，但文字变白导致日期数字与金额不可见；现选中格填充深蓝实底（白字对比度约 5:1），白字清晰可见（系统日历"选中日=实底+白字"标准样式）

## [0.5.2] - 2026-08-16

### 修复（明细页）

- **月历热力图末行格子超宽**：月末最后一行不满 7 格时，剩余日期格平分整行宽度导致比正常格宽数倍（如 2026-08 末行仅 30/31 两格，各为正常格 3.5 倍宽）；现末行补空白占位格，所有日期格与上行等宽

## [0.5.1] - 2026-08-16

明细页 / 统计页 UI 修复（11 项）🔧

### 修复（明细页）

- **切月后选中日不跟随**：点 ‹/› 切月后热力图显示新月份，但下方明细仍停留在旧月份选中日；现选中日自动同步到目标月同日（月末自动 clamp，与系统日历一致）
- **流水模式无法切月**：流水模式新增月份切换条，与日历模式共享月份状态，无需切回日历模式换月
- **流水模式缺失转账**：转账现与收支按时间倒序混排进流水（筛选"支出/收入"时不显示转账；搜索匹配转账备注），日历/流水两模式数据一致
- **记账人标识缺失**：多成员账本下明细页两种模式的记录行均显示记账人标签（与首页一致）
- 搜索框有内容时显示 ✕ 一键清除

### 修复（统计页）

- **预算对比数据错乱**：切到「近7天/年度」时预算卡用 7 天/全年支出与**月预算**对比，百分比与超支判断全错；现预算卡仅在「本月」视图显示
- **总览金额溢出**：总资产/支出/收入/结余大金额自适应字号，不再换行截断
- **饼图中心金额格式**：裸整数（12346）改为千分位格式，与全 App 一致
- **饼图图例增强**：每行显示金额与占比（原来只有占比）；分类超过 8 项自动聚合为「其他」，卡片不再无限拉长
- **趋势图标签截断**：近30天模式 30 个标签挤在一行被截断；标签改在 SVG 内按柱心对齐绘制（抽样显示），任意数量不再截断
- **趋势图宽屏拉伸**：viewBox 固定 320 宽导致宽屏柱子变形；改为 onLayout 实测宽度自适应

## [0.5.0] - 2026-08-16

多成员账本体验 👨‍👩‍👧（记账人标识 / 成员筛选 / 成员统计 / 成员管理）

### 新增：记账人标识

- 记录行显示记账人标签（头像 emoji + 昵称，成员专属配色），一眼分清谁记的账
- 多成员账本自动显示；单成员/未登录不显示，界面零打扰
- 成员配色按 userId 全家一致（同一人在所有设备同色）

### 新增：统计页成员维度

- **成员筛选**：统计页顶部横向筛选 chips（全部 / 各成员），总览/饼图/排行/趋势全部按成员过滤
- **成员支出排行**：各成员支出金额、占比、对比条，点击任一行直接切换为该成员视图
- 数据库查询层支持 userId 过滤（区间汇总/分类汇总/按天汇总）

### 新增：成员管理

- **owner 可移除成员**（`DELETE /api/family/members/:userId`），二次确认，被移除者历史记录保留在账本
- **修改自己的昵称/头像**（`PUT /api/me`）：12 个可选头像，保存后全家设备同步更新
- 家庭管理弹窗：成员行显示创建者/成员徽标，owner 行内「移除」按钮

### 修复（服务端）

- **`/api/me` 路由 404**：原 `/me` 误挂在 `/api/auth` 下（实际路径 `/api/auth/me`），客户端调 `/api/me` 返回「接口不存在」。已拆分为独立 `meRouter` 正确挂载到 `/api`
- 同步完成后主动刷新成员缓存并广播 `SYNC_DONE` 事件（原事件从未发出，管理页同步状态不刷新）

### 变更

- 登录/加入家庭/同步后自动拉取成员列表缓存（`sync.members_json`）；退出登录清空
- 家庭资料修改后回读服务端，本地昵称/头像即时更新

## [0.4.0] - 2026-08-16

家庭共享账本（NAS Docker 自托管后端）🏠

### 新增：后端（server/）

- **Node.js + Express + better-sqlite3 + JWT**，零外部依赖，单文件数据库，Docker 一键部署到 NAS
- **多用户**：用户名密码注册/登录（bcrypt 哈希 + JWT 7 天），登录限流（同 IP 5 次/分钟）
- **家庭公共账本**：创建家庭生成 6 位邀请码，家人凭码加入；owner 可重置邀请码；成员列表展示
- **增量同步 API**：`/api/sync/push`（逐条 LWW upsert）+ `/api/sync/pull`（since 光标增量拉取），zod 入参校验，家庭数据强制隔离
- **Docker**：多阶段 alpine 构建 + tini 信号处理 + healthcheck + volume 持久化（`server/data/`）；NAS 部署指南见 `server/README.md`
- 全链路自测脚本 `server/scripts/selftest.ps1`（注册/建家/加入/push/pull/LWW 冲突/401 全部通过）

### 新增：APP 家庭同步（本地优先）

- **本地 schema v0.3 迁移**：全部实体增加 uuid / user_id / updated_at / deleted（墓碑），旧数据自动回填 uuid
- **删除改软删除**（墓碑）：删除记录/账户/转账等在全家设备上同步生效；90 天前墓碑自动清理
- **同步引擎**（`src/sync/`）：push 本地水位后变更 → pull 服务端增量 → 本地 LWW upsert；uuid 对齐，账户 uuid ↔ 本地 id 自动映射
- **自动同步时机**：启动、记账/导入后 5 秒 debounce、手动「立即同步」；断网静默失败下次自动重试
- **管理页「家庭同步」区块**：服务器地址（连接探活）、登录/注册弹窗、家庭管理（创建/邀请码加入/成员列表/重置邀请码/退出）、上次同步时间
- **可选功能**：不配置服务器则完全本地使用，与旧版无差异；登录后本地历史记录自动归属当前用户
- 冲突策略：整条 last-write-wins（updatedAt 新者胜），家庭场景简单可靠

### 变更

- 备份格式升级 v3（实体含 uuid/updatedAt），**导入兼容 v2** 旧备份
- 管理页新增「家庭同步」区块（数据备份之前）

## [0.3.0] - 2026-08-16

UI 缺陷修复 + 代码质量清理 🔧

### 修复（UI）

- **全面屏适配**：底部导航适配 home indicator 安全区（动态 inset）；底部弹窗改用 SafeAreaView；Toast 位置随状态栏高度自适应
- **键盘遮挡**：首页备注输入聚焦时自动滚回顶部；管理页各弹窗「保存」按钮移入滚动区，键盘弹出不再遮挡
- **热力图布局**：月历格子改按周分行 flex 平分（替代 12.2% 经验值），任意屏宽不再溢出
- **排行条误导**：统计页分类排行改相对最大值归一化（原来占比 >33% 一律顶满）
- **转账行溢出**：长账户名自动省略号截断；流水月汇总卡窄屏收缩
- **趋势图空状态**：无数据时显示提示而非空柱
- **弹窗动画魔数**：起始偏移改用屏幕高度计算，小屏不露馅

### 优化（可访问性）

- 底部导航 / 收支切换 / 数字键盘 / 分类与账户选择 / 删除按钮 / 日历格子等补齐 `accessibilityRole/Label/State`
- 次级说明文字对比度提升至 WCAG AA（#9E9E9E → #857F78）

### 优化（性能）

- **流水明细虚拟化**：流水模式改 FlatList（日期头 + 记录行拍平），整月大数据不再卡顿
- 核心组件 React.memo（数字键盘 / 分类选择 / 账户选择 / 记录行）
- **修复双重刷新**：首页记账后统一走事件通道刷新，删除手动重复查询
- 今日明细合并为单个列表组件（原来每条记录单独包一层）

### 优化（代码质量）

- 设计令牌补齐：语义色（热力 4 档 / 待报销 / 收入徽标 / 遮罩）+ 字号档位，全量替换硬编码颜色与字号
- 版本号单一来源：关于页改读 `expo-constants`（不再硬编码字符串）
- 新增公共 hook：`useToast`（三处重复 Toast 状态收编，ref 模式防定时器泄漏）、`useDeleteRecord`（删除确认收编）
- 导入失败可见：附属数据（账户/转账/周期/分类）写入失败计数，导入完成提示汇总
- 数据加载失败提示：统计页 / 管理页 / 明细页查询失败不再静默空白
- 清理死代码：删除未使用的 `useLedger.ts`、`void xxx;` 占位、`showReimburse` 无效 prop
- 新增依赖：expo-constants（读版本号）

## [0.2.3] - 2026-08-15

交互手感打磨 ✨

### 优化

- 底部弹窗支持**下滑关闭** + 淡入淡出动画（背景渐隐 + 弹窗滑出）
- 弹窗把手扩大触摸热区，下滑更易触发，且不与内部表单滚动冲突
- 空状态美化：列表空数据时显示 📭 表情 + 引导文案
- 月历热力图「今天」增加圆点标记 + 强调边框，一眼定位今日
- 统计饼图图例改为**双列布局**，分类多时不再拉长页面

## [0.2.2] - 2026-08-15

体验与健壮性修复 🔧

### 修复

- 趋势柱状图自适应：30 天数据时柱子不再重叠（动态柱宽 + 标签自动抽样）
- 金额格式化改用纯手写千分位实现，移除 `toLocaleString` 依赖（Hermes 一致性）
- 切换底部标签**不再重挂载页面**，保留滚动位置、避免重复查库闪烁
- 周期记账/账户/转账弹窗支持内部滚动，长表单不再溢出
- 数字键盘按键对齐改为 `space-between`，间距更均匀
- 删除记录/转账增加二次确认，避免误删
- 清理 `getRangeSummary` 的脆弱写法

## [0.2.1] - 2026-08-15

首页交互优化 🎨

### 优化

- 分类宫格改为**横向滑动一行**（圆形色块 + 表情 + 文字，选中填充分类色）
- 记账卡片**置顶**，进入即记，无需滚动
- 数字键盘与「记一笔」按钮收进同一组合，按钮紧贴键盘（间距 8px）
- 金额字号自适应（`adjustsFontSizeToFit`），长金额不溢出
- 今日总览精简为一行 4 项紧凑卡片（今日支出/收入/本月支出/总资产）

## [0.2.0] - 2026-08-15

功能大升级（对齐随手记核心能力）🚀

### 新增

- **账户管理**：现金/银行卡/信用卡/支付宝/微信等账户，每笔记录关联账户，总资产与账户余额自动计算，支持初始余额
- **账户转账**：账户间转账不计收支，明细页可查看/删除转账记录
- **周期记账**：每天/每周/每月/每年自动生成记录（工资、房租、订阅等），可启停、删除
- **报销管理**：记账时标记"待报销"，管理页汇总待报销金额，支持一键全部核销
- **流水明细页**：月历热力图 + 流水列表双模式，按收支类型筛选、备注/分类搜索，按天分组展示，显示账户与报销状态
- **统计增强**：总资产/结余总览、预算 vs 实际对比、支出分类排行 Top5、近 7 天/30 天/12 个月趋势
- **自定义分类**：可新增/删除自定义分类（表情 + 颜色 + 归属支出/收入）
- **Excel 导出**：CSV 格式（Excel 可直接打开，带 BOM），与 JSON 完整备份并存
- **数据迁移**：v0.1 旧数据自动迁移（记录归入默认"现金"账户）

### 变更

- 底部导航重构为 4 个标签：记账 / 明细 / 统计 / 管理
- 预算进度条增强：显示已用/剩余金额与百分比，超支标红
- 内置支出分类扩展至 9 个（新增居住/医疗/教育）

### 技术

- 数据库 schema 升级：accounts / transfers / recurring_rules / custom_categories 四张新表，支持自动迁移
- 备份格式升级至 v2（含账户/转账/周期规则/自定义分类）
